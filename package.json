{
  "name": "cutd-dex",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "sync:force": "node scripts/sync.mjs --force",
    "data": "node scripts/build.mjs",
    "predev": "node scripts/build.mjs",
    "dev": "vite",
    "prebuild": "node scripts/build.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"scripts/test/*.test.mjs\""
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "sharp": "^0.33.5",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=22"
  }
}
