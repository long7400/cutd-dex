{
  "name": "cutd-dex",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "sync:force": "node scripts/sync.mjs --force",
    "data": "node scripts/build.mjs && node scripts/build-tool.mjs",
    "predev": "node scripts/build.mjs && node scripts/build-tool.mjs",
    "dev": "vite",
    "prebuild": "node scripts/build.mjs && node scripts/build-tool.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test --test-force-exit --test-timeout=120000 \"scripts/test/*.test.mjs\""
  },
  "devDependencies": {
    "acorn": "^8.18.0",
    "acorn-walk": "^8.3.5",
    "esbuild": "^0.28.2",
    "jsdom": "^25.0.1",
    "sharp": "^0.35.4",
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=22"
  },
  "homepage": "https://long7400.github.io/cutd-dex/"
}
